service shirakami
{
    disable     = no
    type        = UNLISTED
    socket_type = stream
    protocol    = tcp
    port        = 13370
    wait        = no
    user        = fubuki
    server      = /app/run
}